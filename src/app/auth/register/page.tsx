import { UserRegisterForm } from "@/components/UserRegisterForm";

export default function RegisterPage() {
      return (
            <main className="w-full h-auto flex justify-center items-center py-[80px] bg-gradient-to-br from-gray-50 to-gray-200">
                  <UserRegisterForm />
            </main>
      );
}